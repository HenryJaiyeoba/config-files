fork = false
# (macOS only) transparent: Transparent decorations including a transparent bar.
# (macOS only) buttonless: All decorations, but without quit, minimize or fullscreen buttons.
# frame = "full"
frame = "transparent"
idle = true
maximized = false
# neovim-bin = "/usr/bin/nvim" # in reality found dynamically on $PATH if unset
no-multigrid = false
# srgb = false
tabs = true
theme = "auto"
title-hidden = true
# If vsync is disabled, you can set `neovide_refresh_rate` to the refresh
# rate of your physical hardware. I set the refresh rate in the neobean
# options.lua file
# https://github.com/neovide/neovide/issues/2093
vsync = true
wsl = false
icon ="/Users/henryjaiyeoba/Documents/neovide-icon.icns"
[font]
normal = "Iosevka Nerd Font Propo"
size = 20.0
